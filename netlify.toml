# Netlify configuration for Next.js 16
[build]
  command = "npm run build"
  publish = ".next"

# Automatically use the Essential Next.js plugin
[build.environment]
  NODE_VERSION = "20"

# Next.js specific redirects (handled by Essential Next.js plugin)
# No manual redirects needed - plugin handles it
